// import { getExpensesByUserId } from "../api/queries";
// import { apolloClient } from 'vue-apollo';
// import axios from 'axios';

const state = () => ({
    expenses: [],
})

const getters = {
    expenses: state => state.expenses,
}

const actions = {
}

const mutations = {

}